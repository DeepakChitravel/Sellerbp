"use client";
import React from "react";

export default function WeddingSeatDesigner({ eventId }: { eventId: number }) {
  return <div className="p-6">Wedding Layout Builder (WIP) — event {eventId}</div>;
}
